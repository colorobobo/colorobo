modifier les fichiers en local (bureau/coloroboweb/colorobo)
dans l'appli github desktop : se connecter avec Colorobobo (préférences), faire commit to master
dans l'appli github desktop : repository > push
vérifier en se logant à github que les fichiers sont bien ok

Pour nouveau projet : 
Créer un compte GitHub
Créer un repository
Y mettre les fichiers avec un premier upload
Dans settings > GitHub pages, on peut voir si elles sont en ligne (le repository doit être public)
Dans appli GitHub faire file > clone repository : préciser le répertoire en local (1er onglet - localpath) et le répertoire distant (3ème onglet - url)
On peut ensuite modifier en local et pusher dans la branche adéquat

Sur l'appstore Connect, ajouter une app. Il pour l'identifiant de lot, il faut cliquer sur le lien pour aller sur le portail des développeurs et ajouter un AppId avec le bundle setter dans xcode (com.marieb.bibinary). Ensuite faut refermer et refaire ajouter une app et l'appId tout juste créé apparaitra.



